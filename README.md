# Security-Camera-with-Motion-Detection
A Python app using OpenCV to detect faces and bodies. It starts recording when motion is detected and saves the video with a timestamp. Blue rectangles highlight detected objects.
